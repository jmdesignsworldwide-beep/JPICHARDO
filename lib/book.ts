/**
 * Disponibilidad del libro "Comenzando Mi Viaje" — UNA sola fuente de verdad.
 *
 * El libro AÚN NO está en Amazon. El botón muestra "Próximamente en Amazon"
 * en estado espera premium.
 *
 * Para activarlo cuando salga:
 *   1) pega la URL de Amazon en BOOK_AMAZON_URL
 *   2) pon BOOK_AVAILABLE = true
 * El botón pasa automáticamente a "Comprar en Amazon" (target _blank,
 * rel noopener noreferrer). No hay que tocar ningún otro archivo.
 */
export const BOOK_AVAILABLE = false;
export const BOOK_AMAZON_URL = ''; // ej. 'https://www.amazon.com/dp/XXXXXXXXXX'
